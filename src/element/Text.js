import React from "react";
import styled from 'styled-components';

const Text = (props) => {
  return (
    null
  )
}

Text.defaultProps = {
  // margin: "20px 30px",
  // display: "block",
  // overflow: 'hidden',
  // textOverflow: ellipsis,
  // whiteSpace: nowrap,
};

export default Text;